using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaneHeight : MonoBehaviour
{
    public GameObject planeHeight;

    // Update is called once per frame
    void Update()
    {
        //if (TriggerCollisionDetection.isSinglePlayer)
        //{
        //    planeHeight.transform.position= new Vector3(planeHeight.transform.position.x, (-1.65f + 1.4f),planeHeight.transform.position.z);
        //}
        //if (TriggerCollisionDetection.isPlayerWithBoat)
        //{
        //    planeHeight.transform.position = new Vector3(planeHeight.transform.position.x, -2f+1.4f, planeHeight.transform.position.z);
        //}
    }
}
